import { Inter, Lusitana } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const lusitana = Lusitana({
  subsets: ["latin"],
  weight: ["400", "700"],
});
// This file contains the font definitions for the app. It uses the `Inter` and `Lusitana` fonts from Google Fonts.
